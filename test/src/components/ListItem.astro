---
import type { Props as $, ListItem } from "@portabletext/astro/types";
import { usePortableText } from "@portabletext/astro";

type Props = $<ListItem>;

const { getDefaultComponent } = usePortableText(Astro.props.node);
const Li = getDefaultComponent();
---

<Li {...Astro.props}><slot /></Li>

---
import type { BlockProps } from "@portabletext/astro/types";

export type Props = BlockProps;

const props = Astro.props;
const { node, isInline, index, ...attrs } = props;
---

<div data-block="standfirst" {...attrs}>
  <slot />
</div>

---
import type { Props as $, Mark } from "@portabletext/astro/types";
import { usePortableText } from "@portabletext/astro";

type Props = $<Mark>;

const { getDefaultComponent } = usePortableText(Astro.props.node);
const Mrk = getDefaultComponent();
---

<Mrk {...Astro.props}><slot /></Mrk>
